generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model Tag {
  id     String @id
  name   String
  badge  String
  server String
  invite String
  count  Int
}
